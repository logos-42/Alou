{
  "name": "server-filesystem",
  "version": "1.0.0",
  "description": "MCP service configuration for @modelcontextprotocol/server-filesystem",
  "scripts": {
    "start": "npx -y @modelcontextprotocol/server-filesystem"
  }
}